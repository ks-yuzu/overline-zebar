# Refresh the cache when the WSL distribution starts and every five minutes.
@reboot /usr/bin/timeout 60s "$HOME/bin/claude-usage-json" --force >/dev/null 2>&1
*/5 * * * * /usr/bin/timeout 60s "$HOME/bin/claude-usage-json" --force >/dev/null 2>&1
